import React from "react";
import { views } from "@zobi.dev/extension-api";

views.registerView(
  { id: "{{ id }}.example", name: "{{ display_name }}" },
  "sqllab.panels",
  () => <p>{{ display_name }}</p>,
);
