[project]
name = "{{ backend_package }}"
version = "{{ version }}"
license = "{{ license }}"

[tool.zobi_extensions.build]
# Files to include in the extension build/bundle
include = [
    "src/{{ backend_path|replace('.', '/') }}/**/*.py",
]
exclude = []
