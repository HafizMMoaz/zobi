name: Publish @zobi.dev/ag-grid-table

# Publishes only @zobi.dev/ag-grid-table (frontend/plugins/ag-grid-table).

# Trigger manually, or push a package-scoped tag such as ag-grid-table-v1.2.3
on:
  push:
    tags:
      - 'ag-grid-table-v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version bump (patch, minor, major) or an exact version'
        required: true
        default: 'patch'
      dry_run:
        description: 'Pack only, do not publish'
        type: boolean
        required: false
        default: false

concurrency:
  group: publish-ag-grid-table
  cancel-in-progress: false

jobs:
  publish:
    name: '@zobi.dev/ag-grid-table'
    runs-on: ubuntu-latest
    permissions:

      contents: read
      id-token: write # npm provenance

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22.22.0'
          registry-url: 'https://registry.npmjs.org'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Verify package identity
        run: |
          set -euo pipefail
          ACTUAL=$(node -p "require('./plugins/ag-grid-table/package.json').name")
          if [ "$ACTUAL" != "@zobi.dev/ag-grid-table" ]; then
            echo "::error::frontend/plugins/ag-grid-table declares '$ACTUAL', expected '@zobi.dev/ag-grid-table'"
            exit 1
          fi
          echo "$ACTUAL @ $(node -p "require('./plugins/ag-grid-table/package.json').version")"

      - name: Set version
        if: github.event_name == 'workflow_dispatch'
        run: |
          set -euo pipefail
          npm version "${{ inputs.version }}" --no-git-tag-version --allow-same-version --workspace @zobi.dev/ag-grid-table

      - name: Build
        run: |
          npm run prune
          npm run plugins:build

      - name: Pack (dry run)

        if: github.event_name == 'workflow_dispatch' && inputs.dry_run
        working-directory: frontend/plugins/ag-grid-table
        run: npm pack --dry-run

      - name: Publish to npm
        if: github.event_name != 'workflow_dispatch' || !inputs.dry_run
        working-directory: frontend/plugins/ag-grid-table
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --access public
