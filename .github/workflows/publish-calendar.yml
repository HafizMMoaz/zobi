name: Publish @zobi.dev/calendar

# Publishes only @zobi.dev/calendar (frontend/plugins/calendar).

# Trigger manually, or push a package-scoped tag such as calendar-v1.2.3
on:
  push:
    tags:
      - 'calendar-v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version bump (patch, minor, major) or an exact version'
        required: true
        default: 'patch'
      dry_run:
        description: 'Pack only, do not publish'
        type: boolean
        required: false
        default: false

concurrency:
  group: publish-calendar
  cancel-in-progress: false

jobs:
  publish:
    name: '@zobi.dev/calendar'
    runs-on: ubuntu-latest
    permissions:

      contents: read
      id-token: write # npm provenance

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22.22.0'
          registry-url: 'https://registry.npmjs.org'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Verify package identity
        run: |
          set -euo pipefail
          ACTUAL=$(node -p "require('./plugins/calendar/package.json').name")
          if [ "$ACTUAL" != "@zobi.dev/calendar" ]; then
            echo "::error::frontend/plugins/calendar declares '$ACTUAL', expected '@zobi.dev/calendar'"
            exit 1
          fi
          echo "$ACTUAL @ $(node -p "require('./plugins/calendar/package.json').version")"

      - name: Set version
        if: github.event_name == 'workflow_dispatch'
        run: |
          set -euo pipefail
          npm version "${{ inputs.version }}" --no-git-tag-version --allow-same-version --workspace @zobi.dev/calendar

      - name: Build
        run: |
          npm run prune
          npm run plugins:build

      - name: Pack (dry run)

        if: github.event_name == 'workflow_dispatch' && inputs.dry_run
        working-directory: frontend/plugins/calendar
        run: npm pack --dry-run

      - name: Publish to npm
        if: github.event_name != 'workflow_dispatch' || !inputs.dry_run
        working-directory: frontend/plugins/calendar
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --access public
