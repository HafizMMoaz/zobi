"""LLM gateway: provider configuration, Router assembly, and model access.

Other Zobi code should import from ``zobi.llm.service`` only. Everything else
in this package is implementation detail of how providers are stored and how a
LiteLLM ``Router`` is built from them.
"""
